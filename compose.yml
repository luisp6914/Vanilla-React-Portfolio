
services:
  database:
    image: postgres:16
    volumes:
      - type: bind
        source: ./covid.sql
        target: /docker-entrypoint-initdb.d/covid.sql
    env_file:
      - .env
    ports:
      - "5432:5432"
    container_name: database-container


